# Steam tracker application (Python, Docker)

Build environment
----------------

    docker-compose build

Start Application
----------------

    docker-compose up

Start using at
----------------

    http://localhost:8082

Go to ``/createtbl`` to create tables in database.

Go to ``/droptbl`` to drop all tables.
